import { Text, View } from "react-native";

const Buffer = () => {
	return (
		<View
			style={{ height: 30, justifyContent: "center", alignItems: "center" }}
		>
			<Text>Buffer for developement</Text>
		</View>
	);
};

export default Buffer;
